# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.
COMMON_FLAGS="-march=native -O2 -Wall"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"

MAKEOPTS="-j8"

# NOTE: This stage was built with the bindist Use flag enabled
PORTDIR="/var/db/repos/gentoo"
DISTDIR="/var/cache/distfiles"
PKGDIR="/var/cache/binpkgs"

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C

GENTOO_MIRRORS="ftp://mirror.csclub.uwaterloo.ca/gentoo-distfiles/ https://mirror.csclub.uwaterloo.ca/gentoo-distfiles/ http://mirror.csclub.uwaterloo.ca/gentoo-distfiles/ http://www.gtlib.gatech.edu/pub/gentoo rsync://rsync.gtlib.gatech.edu/gentoo https://gentoo.osuosl.org/ http://gentoo.osuosl.org/ https://mirrors.rit.edu/gentoo/ http://mirrors.rit.edu/gentoo/ ftp://mirrors.rit.edu/gentoo/ rsync://mirrors.rit.edu/gentoo/"

USE="X nvidia wifi vulkan vaapi aac aalib acpi alsa bluetooth cjk dbus djvu elogind encode ffmpeg flac fontconfig ftp gif git hddtemp imagemagick imap ipv6 jpeg libnotify lm-sensors matroska mbox mng mp3 mp4 mpeg mplayer mtp musepack ncurses offensive ogg opengl opus fcitx4 pmcia pdf png policykit pulseaudio raw sound svg svga theora threads tiff truetype unicode upower usb vnc vorbis webp x264 -systemd" 

GRUB_PLATFORMS="efi-64"
VIDEO_CARDS="nvidia intel i965"
INPUT_DEVICES="libinput keyboard mouse"

ACCEPT_KEYWORDS="~amd64"
